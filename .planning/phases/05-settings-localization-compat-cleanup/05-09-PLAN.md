---
phase: 05-settings-localization-compat-cleanup
plan: 09
type: execute
wave: 9
depends_on: ["05-08"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-04
  - COMPAT-01
  - COMPAT-02
  - COMPAT-03
  - COMPAT-04
  - BEHAV-01
  - BEHAV-02
  - BEHAV-03
  - BEHAV-04
  - BEHAV-05
must_haves:
  truths:
    - "hemtt build produces zero errors and zero warnings (BBW1 accepted)"
    - "nr6_hal/ directory does not exist"
    - "All 13 Phase 5 requirements are verified satisfied"
    - "ROADMAP.md and STATE.md reflect Phase 5 completion"
    - "All 37 v1.0 requirements across all 5 phases are marked complete"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated roadmap with Phase 5 complete"
      contains: "Phase 5"
    - path: ".planning/STATE.md"
      provides: "Updated state with v1.0 milestone complete"
      contains: "Phase 5"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "All requirement checkboxes marked [x]"
      pattern: "\\[x\\]"
---

<objective>
Final build verification and project milestone completion. Run comprehensive checks against all Phase 5 requirements, update project tracking documents, and verify the entire v1.0 refactor is complete.

Purpose: This is the closing plan for the entire v1.0 NR6-HAL refactor. It confirms all 37 requirements are satisfied and updates all tracking artifacts.
Output: Clean build verification, updated ROADMAP.md, STATE.md, and REQUIREMENTS.md.
</objective>

<execution_context>
@C:\Users\jakub\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jakub\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive Phase 5 requirement verification</name>
  <files>
    (no files modified — verification only)
  </files>
  <action>
Run each verification check and document results:

**COMPAT-04: nr6_hal/ removed**
```bash
test ! -d nr6_hal && echo "PASS: nr6_hal deleted" || echo "FAIL: nr6_hal still exists"
```

**BUILD gate: HEMTT clean build**
```bash
hemtt build 2>&1 | tail -20
# Must be zero errors, zero warnings (BBW1 accepted)
```

**SET-01: CBA settings registered**
```bash
grep -c "CBA_fnc_addSetting" addons/core/initSettings.inc.sqf
# Should be >= 90
```

**SET-02: All configurable options exposed**
```bash
# Verify settings count matches CfgVehicles argument count
grep -c "class EGVAR\|class GVAR" addons/missionmodules/CfgVehicles.hpp
```

**SET-03: stringtable.xml exists with English entries**
```bash
test -f addons/main/stringtable.xml && grep -c "<English>" addons/main/stringtable.xml
# Should be >= 200
```

**SET-04: LSTRING/CSTRING macros used**
```bash
grep -c "LSTRING\|LLSTRING\|CSTRING" addons/core/initSettings.inc.sqf addons/missionmodules/CfgVehicles.hpp
```

**COMPAT-01: Old classnames mapped**
```bash
grep -c "NR6_HAL_" addons/compat_nr6hal/config.cpp
# Should be 9
```

**COMPAT-02: Function aliases**
```bash
grep -c "HAL_" addons/compat_nr6hal/XEH_postInit.sqf
# Should be >= 41
```

**COMPAT-03: Overall compat integrity**
```bash
# Verify compat addon builds into a PBO
hemtt build 2>&1 | grep -i "compat"
```

**Bare legacy variable check (cross-phase):**
```bash
grep -rn "RydHQ_\|RydxHQ_\|RYD_\|RydBB_\|RHQ_" addons/ | grep -v compat | grep -v "Originally from" | grep -v "^Binary" | grep -v "\.md:" | wc -l
# Must be 0
```

**Bare HAL_* function handle check:**
```bash
grep -rn "call HAL_\|spawn HAL_" addons/ | grep -v compat | grep -v "^Binary" | wc -l
# Must be 0
```

**BEHAV-01..05: Test scripts exist**
```bash
ls tests/test_BEHAV_*.sqf | wc -l
# Should be 5
```

**Total PREP count:**
```bash
grep -c "PREP(" addons/*/XEH_PREP.hpp
# Document for the summary
```

Document all results. If ANY check fails, fix the issue before proceeding to Task 2.
  </action>
  <verify>
    <automated>cd C:/NAUKA/NR6-HAL && hemtt build 2>&1 | tail -3 && test ! -d nr6_hal && echo "COMPAT-04 OK" && test -f addons/core/initSettings.inc.sqf && echo "SET-01 OK" && test -f addons/main/stringtable.xml && echo "SET-03 OK" && ls tests/test_BEHAV_*.sqf 2>/dev/null | wc -l</automated>
  </verify>
  <done>All 13 Phase 5 requirements verified. HEMTT build clean. No legacy variable names or bare function handles remain.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md, STATE.md, and REQUIREMENTS.md</name>
  <files>
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
1. **Update REQUIREMENTS.md:**
   - Mark all Phase 5 requirements as complete: SET-01..04, COMPAT-01..04, BEHAV-01..05
   - Change `- [ ]` to `- [x]` for each
   - Verify all 37 v1 requirements are now `[x]`

2. **Update ROADMAP.md:**
   - Mark Phase 5 as complete in the progress table
   - Update the plan list with checkmarks for all 9 plans
   - Set all 5 phases to complete
   - Update progress: 5/5 phases complete, all plans complete

3. **Update STATE.md:**
   - Set `status: complete`
   - Set `completed_phases: 5`
   - Set `completed_plans: 32` (or actual total across all phases)
   - Set `percent: 100`
   - Update `stopped_at` to "v1.0 milestone complete"
   - Add session continuity note about v1.0 completion
   - Add final performance metrics
  </action>
  <verify>
    <automated>cd C:/NAUKA/NR6-HAL && grep -c "\[x\]" .planning/REQUIREMENTS.md && grep "percent" .planning/STATE.md</automated>
  </verify>
  <done>All project tracking documents updated. All 37 v1 requirements marked complete. ROADMAP shows 5/5 phases complete. STATE shows 100% progress. v1.0 NR6-HAL refactor milestone is DONE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | This plan only verifies and updates documentation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-20 | Repudiation | Premature completion marking | mitigate | All 13 requirements verified by automated checks before marking complete |
</threat_model>

<verification>
- `hemtt build` exits 0
- All 13 Phase 5 requirement checks pass
- REQUIREMENTS.md has 37 `[x]` entries
- ROADMAP.md shows all 5 phases complete
- STATE.md shows 100% progress
</verification>

<success_criteria>
v1.0 NR6-HAL refactor is verified complete. All 37 requirements satisfied. All tracking documents updated. The project builds cleanly with zero legacy code remaining in the runtime path.
</success_criteria>

<output>
After completion, create `.planning/phases/05-settings-localization-compat-cleanup/05-09-SUMMARY.md`
</output>
