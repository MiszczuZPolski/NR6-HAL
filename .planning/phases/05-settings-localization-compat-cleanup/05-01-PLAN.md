---
phase: 05-settings-localization-compat-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - addons/core/functions/fnc_varInit.sqf
  - addons/core/XEH_PREP.hpp
  - addons/core/functions/fnc_init.sqf
  - addons/common/functions/fnc_timeDisOP.sqf
  - addons/common/functions/fnc_timeEnOP.sqf
  - addons/common/functions/fnc_timeFaster.sqf
  - addons/common/functions/fnc_timeSlower.sqf
  - addons/common/XEH_PREP.hpp
  - addons/hal_tasking/functions/fnc_taskMenu.sqf
  - addons/hal_tasking/XEH_PREP.hpp
  - addons/hal_data/CfgRadio.hpp
  - addons/hal_data/config.cpp
autonomous: true
requirements:
  - COMPAT-04
must_haves:
  truths:
    - "VarInit.sqf unique variable defaults (artillery arrays, smoke/flare muzzles) are preserved in fnc_varInit.sqf"
    - "TimeM scripts and TaskMenu are PREP'd functions in addons/"
    - "Sound assets live in hal_data/Sound/ and CfgRadio paths reference the new PBO location"
    - "fnc_init.sqf no longer loads any file via preprocessFile/execVM from nr6_hal/ for these items"
  artifacts:
    - path: "addons/core/functions/fnc_varInit.sqf"
      provides: "VarInit.sqf Section A unique defaults (artillery, smoke, flare arrays)"
      contains: "#include"
    - path: "addons/hal_data/CfgRadio.hpp"
      provides: "351 CfgRadio class entries with updated sound paths"
      contains: "CfgRadio"
    - path: "addons/hal_data/Sound/"
      provides: "All audio assets from nr6_hal/Sound/"
  key_links:
    - from: "addons/core/functions/fnc_init.sqf"
      to: "addons/core/functions/fnc_varInit.sqf"
      via: "call FUNC(varInit) replacing preprocessFile VarInit.sqf"
      pattern: "FUNC\\(varInit\\)"
---

<objective>
Extract VarInit.sqf unique content, TimeM scripts, TaskMenu.sqf, LF/LF.sqf, and Sound assets from nr6_hal/ into addons/. This is the simplest extraction batch — small files with clear destinations, no cross-dependencies.

Purpose: Clear the easy files first, establishing the extraction pattern. VarInit.sqf must land first because later plans depend on understanding what unique defaults it provides vs. what is already in addons/.
Output: 6-8 new fnc_*.sqf files in addons/, Sound assets in hal_data, CfgRadio config migrated, fnc_init.sqf loader lines updated.
</objective>

<execution_context>
@C:\Users\jakub\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jakub\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-settings-localization-compat-cleanup/05-CONTEXT.md
@.planning/phases/05-settings-localization-compat-cleanup/05-RESEARCH.md
@addons/core/functions/fnc_init.sqf
@addons/core/XEH_PREP.hpp
@addons/common/XEH_PREP.hpp
@addons/hal_data/config.cpp
@addons/hal_data/XEH_PREP.hpp
@nr6_hal/VarInit.sqf
@nr6_hal/TaskMenu.sqf
@nr6_hal/LF/LF.sqf
@nr6_hal/TimeM/DisOP.sqf
@nr6_hal/TimeM/EnOP.sqf
@nr6_hal/TimeM/TimeFaster.sqf
@nr6_hal/TimeM/TimeSlower.sqf
@nr6_hal/config.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract VarInit.sqf unique content + small utility scripts</name>
  <files>
    addons/core/functions/fnc_varInit.sqf
    addons/core/XEH_PREP.hpp
    addons/common/functions/fnc_timeDisOP.sqf
    addons/common/functions/fnc_timeEnOP.sqf
    addons/common/functions/fnc_timeFaster.sqf
    addons/common/functions/fnc_timeSlower.sqf
    addons/common/XEH_PREP.hpp
    addons/hal_tasking/functions/fnc_taskMenu.sqf
    addons/hal_tasking/XEH_PREP.hpp
  </files>
  <action>
1. Read nr6_hal/VarInit.sqf lines 1-161 (Section A). Compare with existing addons/ code:
   - Lines 162-1084 (weapon class arrays) are ALREADY in addons/hal_data/functions/fnc_initWeaponClasses.sqf — skip.
   - Lines 1085-1211 (function handle compilation) will be DELETED — CBA PREP replaces them. Do NOT extract these.
   - Lines 1-161: Identify content NOT already duplicated in addons/ (artillery arrays like RydHQ_Howitzer, smoke/flare muzzle arrays, misc defaults). Extract only the UNIQUE content.
   
2. Create `addons/core/functions/fnc_varInit.sqf`:
   - Add `#include "..\script_component.hpp"` header
   - Add comment: `// Originally from nr6_hal/VarInit.sqf (Section A: variable defaults)`
   - Add comment: `// NOTE: Weapon class arrays (VarInit lines 162-1084) already in hal_data/fnc_initWeaponClasses.sqf`
   - Add comment: `// NOTE: Function handle compilation (VarInit lines 1085-1211) replaced by CBA PREP`
   - Paste ONLY the unique variable default assignments from lines 1-161
   - The variable names in these lines should already use GVAR/EGVAR macros IF they were Phase-4-renamed. If they still use raw RydHQ_*/RYD_* names, convert them using the rename-map.json at `.planning/phases/04-variable-namespacing/rename-map.json`. Look up each legacy name in the map's `legacy_name` field and replace with `new_macro_owner_form` or `new_macro_extern_form` depending on which addon this file is in (core).
   - Register in addons/core/XEH_PREP.hpp: add `PREP(varInit);` BEFORE the existing `PREP(init);` line (varInit must compile before init).

3. Read nr6_hal/LF/LF.sqf (10 lines). Compare with addons/common/functions/fnc_LF.sqf (if it exists — check `PREP(LF)` in common XEH_PREP.hpp). If LF.sqf is a duplicate or caller, either skip (already migrated) or merge. If it's the same function, just delete nr6_hal/LF/LF.sqf. If different, extract it with a distinct name.

4. Extract TimeM scripts (4 files, each 3-5 lines) to addons/common/functions/:
   - nr6_hal/TimeM/DisOP.sqf -> fnc_timeDisOP.sqf
   - nr6_hal/TimeM/EnOP.sqf -> fnc_timeEnOP.sqf
   - nr6_hal/TimeM/TimeFaster.sqf -> fnc_timeFaster.sqf
   - nr6_hal/TimeM/TimeSlower.sqf -> fnc_timeSlower.sqf
   - Each file: add `#include "..\script_component.hpp"` header, add origin comment
   - If any contain raw legacy variable names, convert using rename-map.json
   - Register all 4 in addons/common/XEH_PREP.hpp

5. Extract nr6_hal/TaskMenu.sqf (37 lines) to addons/hal_tasking/functions/fnc_taskMenu.sqf:
   - Add `#include "..\script_component.hpp"` header, origin comment
   - Convert any legacy variable names using rename-map.json
   - Register in addons/hal_tasking/XEH_PREP.hpp

6. Run `hemtt build` to verify zero errors. Fix any issues.
  </action>
  <verify>
    <automated>cd C:/NAUKA/NR6-HAL && hemtt build 2>&1 | tail -5</automated>
  </verify>
  <done>fnc_varInit.sqf exists with unique VarInit.sqf defaults. 4 TimeM functions and fnc_taskMenu.sqf exist as PREP'd functions. All registered in XEH_PREP.hpp. HEMTT build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Sound assets and CfgRadio config to hal_data</name>
  <files>
    addons/hal_data/Sound/
    addons/hal_data/CfgRadio.hpp
    addons/hal_data/config.cpp
    addons/core/functions/fnc_init.sqf
  </files>
  <action>
1. Copy the entire `nr6_hal/Sound/` directory to `addons/hal_data/Sound/`:
   ```bash
   cp -r nr6_hal/Sound addons/hal_data/Sound
   ```

2. Extract the CfgRadio section from nr6_hal/config.cpp (lines ~268-4450, the entire `class CfgRadio` block) into `addons/hal_data/CfgRadio.hpp`.

3. In the extracted CfgRadio.hpp, do a global find-replace:
   - Replace ALL occurrences of `\NR6_HAL\Sound\` with `\z\hal\addons\hal_data\Sound\`
   - Verify the replacement count is approximately 351 (the number of sound entries)
   - The new path `\z\hal\addons\hal_data\` matches hal_data's $PBOPREFIX$ (`z\hal\addons\hal_data`)

4. Add `#include "CfgRadio.hpp"` to addons/hal_data/config.cpp inside the `class CfgPatches` block or at the appropriate config scope level. CfgRadio is a root-level config class, so include it outside CfgPatches but inside the config.cpp file.

5. Update fnc_init.sqf: Remove or comment out any remaining `preprocessFile` / `execVM` lines that reference nr6_hal/ paths for the files extracted in Task 1 (VarInit.sqf loader if still present, TaskMenu.sqf loader, LF/LF.sqf loader, TimeM loaders). Add `call FUNC(varInit);` where the VarInit.sqf loader was.

6. Run `hemtt build` to verify the Sound assets are included in the PBO and CfgRadio compiles clean.
  </action>
  <verify>
    <automated>cd C:/NAUKA/NR6-HAL && hemtt build 2>&1 | tail -5 && test -d addons/hal_data/Sound && echo "Sound dir exists" && grep -c "z\\\\hal\\\\addons\\\\hal_data\\\\Sound" addons/hal_data/CfgRadio.hpp</automated>
  </verify>
  <done>Sound/ directory in hal_data with all .ogg files. CfgRadio.hpp has ~351 entries with updated paths. hal_data/config.cpp includes CfgRadio.hpp. fnc_init.sqf calls FUNC(varInit) instead of preprocessFile VarInit.sqf. HEMTT build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File path references | CfgRadio sound paths must match actual PBO prefix |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-01 | Tampering | CfgRadio paths | mitigate | Verify replacement count matches expected 351 entries |
| T-05-02 | Denial | VarInit unique defaults | mitigate | Compare with existing addons/ code to avoid duplicating already-migrated arrays |
</threat_model>

<verification>
- `hemtt build` exits 0
- `grep -r "nr6_hal" addons/core/functions/fnc_init.sqf` returns zero hits for VarInit/TaskMenu/LF/TimeM loaders
- `test -f addons/core/functions/fnc_varInit.sqf` passes
- `test -f addons/hal_data/CfgRadio.hpp` passes
- `grep "PREP(varInit)" addons/core/XEH_PREP.hpp` returns a match
</verification>

<success_criteria>
All small utility files from nr6_hal/ (VarInit unique content, TimeM, TaskMenu, LF) are PREP'd functions in addons/. Sound assets and CfgRadio config are in hal_data with correct paths. fnc_init.sqf loaders are updated. HEMTT build is clean.
</success_criteria>

<output>
After completion, create `.planning/phases/05-settings-localization-compat-cleanup/05-01-SUMMARY.md`
</output>
