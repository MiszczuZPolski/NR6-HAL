---
phase: 05-settings-localization-compat-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - addons/hal_boss/functions/fnc_boss.sqf
  - addons/hal_boss/XEH_PREP.hpp
  - addons/hal_hac/functions/fnc_desperation.sqf
  - addons/hal_hac/XEH_PREP.hpp
  - addons/core/functions/fnc_front.sqf
  - addons/core/functions/fnc_HQSitRepB.sqf
  - addons/core/functions/fnc_HQSitRepC.sqf
  - addons/core/functions/fnc_HQSitRepD.sqf
  - addons/core/functions/fnc_HQSitRepE.sqf
  - addons/core/functions/fnc_HQSitRepF.sqf
  - addons/core/functions/fnc_HQSitRepG.sqf
  - addons/core/functions/fnc_HQSitRepH.sqf
  - addons/core/functions/fnc_init.sqf
  - addons/core/XEH_PREP.hpp
  - addons/hal_hac/functions/fnc_flanking.sqf
  - addons/hal_hac/functions/fnc_hqOrders.sqf
  - addons/hal_hac/functions/fnc_hqOrdersDef.sqf
  - addons/hal_hac/functions/fnc_hqOrdersEast.sqf
  - addons/hal_hac/functions/fnc_hqReset.sqf
  - addons/hal_hac/functions/fnc_lhq.sqf
  - addons/hal_hac/functions/fnc_lPos.sqf
  - addons/hal_hac/functions/fnc_reloc.sqf
  - addons/hal_hac/functions/fnc_rev.sqf
  - addons/hal_hac/functions/fnc_sfIdleOrd.sqf
  - addons/hal_hac/functions/fnc_spotScan.sqf
autonomous: false
requirements:
  - COMPAT-04
  - BEHAV-01
  - BEHAV-02
must_haves:
  truths:
    - "Boss.sqf main AI loop is a PREP'd function in hal_boss callable as EFUNC(hal_boss,boss)"
    - "Front.sqf and all 7 HQSitRep[B-H].sqf are PREP'd in core"
    - "HQ-level command scripts (HQOrders, HQOrdersDef, HQReset, Flanking, etc.) are PREP'd in hal_hac"
    - "fnc_init.sqf dynamic HQSitRep dispatch uses EFUNC references instead of compile preprocessFile"
    - "No remaining preprocessFile/execVM calls to nr6_hal/ for Boss/Front/HQSitRep/HQ-command scripts"
  artifacts:
    - path: "addons/hal_boss/functions/fnc_boss.sqf"
      provides: "Main AI commander loop (2021 lines)"
      contains: "#include"
    - path: "addons/core/functions/fnc_front.sqf"
      provides: "Front-line detection"
      contains: "#include"
    - path: "addons/hal_hac/functions/fnc_hqOrders.sqf"
      provides: "HQ attack order dispatch (1187 lines)"
      contains: "#include"
  key_links:
    - from: "addons/core/functions/fnc_init.sqf"
      to: "addons/hal_boss/functions/fnc_boss.sqf"
      via: "EFUNC(hal_boss,boss) replacing call Boss"
      pattern: "EFUNC\\(hal_boss,boss\\)"
    - from: "addons/core/functions/fnc_init.sqf"
      to: "HQSitRep[A-H] functions"
      via: "Dynamic dispatch variables populated with EFUNC references"
      pattern: "_HQSitRep"
---

<objective>
Extract Boss.sqf (2021 lines), Front.sqf, Desperation.sqf, HQSitRep[B-H].sqf (7 copies), and HQ-level command scripts (HQOrders, HQOrdersDef, HQOrdersEast, HQReset, Flanking, LHQ, LPos, Reloc, Rev, SFIdleOrd, SpotScan) from nr6_hal/ into addons/. This is the command-and-control layer of the AI.

Purpose: Boss.sqf is the heart of the HAL AI commander — its extraction is the most behavior-sensitive operation in Phase 5. The HQ-level scripts are its direct callees. All must land before the tactical behavior scripts (Plan 03) since HQOrders.sqf dispatches to them.
Output: ~20 new fnc_*.sqf files across hal_boss, hal_hac, and core. fnc_init.sqf updated to use EFUNC references for Boss and HQSitRep dispatch.
</objective>

<execution_context>
@C:\Users\jakub\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jakub\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-settings-localization-compat-cleanup/05-CONTEXT.md
@.planning/phases/05-settings-localization-compat-cleanup/05-RESEARCH.md
@.planning/phases/05-settings-localization-compat-cleanup/05-01-SUMMARY.md
@addons/core/functions/fnc_init.sqf
@addons/core/XEH_PREP.hpp
@addons/hal_boss/XEH_PREP.hpp
@addons/hal_hac/XEH_PREP.hpp
@nr6_hal/Boss.sqf
@nr6_hal/Front.sqf
@nr6_hal/Desperation.sqf
@nr6_hal/VarInit.sqf
@.planning/phases/04-variable-namespacing/rename-map.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract Boss.sqf, Front.sqf, Desperation.sqf, and HQSitRep[B-H].sqf</name>
  <files>
    addons/hal_boss/functions/fnc_boss.sqf
    addons/hal_boss/XEH_PREP.hpp
    addons/core/functions/fnc_front.sqf
    addons/core/functions/fnc_HQSitRepB.sqf
    addons/core/functions/fnc_HQSitRepC.sqf
    addons/core/functions/fnc_HQSitRepD.sqf
    addons/core/functions/fnc_HQSitRepE.sqf
    addons/core/functions/fnc_HQSitRepF.sqf
    addons/core/functions/fnc_HQSitRepG.sqf
    addons/core/functions/fnc_HQSitRepH.sqf
    addons/core/XEH_PREP.hpp
    addons/hal_hac/functions/fnc_desperation.sqf
    addons/hal_hac/XEH_PREP.hpp
  </files>
  <action>
1. **Boss.sqf** (2021 lines) -> `addons/hal_boss/functions/fnc_boss.sqf`:
   - Add `#include "..\script_component.hpp"` header
   - Add comment: `// Originally from nr6_hal/Boss.sqf — main AI commander loop`
   - Copy the entire Boss.sqf content
   - **CRITICAL variable rename step:** Boss.sqf was NOT processed by Phase 4. It still contains raw RydHQ_*/RYD_*/RydxHQ_* variable names. Convert ALL legacy variable names to GVAR()/EGVAR() form using `.planning/phases/04-variable-namespacing/rename-map.json`:
     - For each legacy name found, look up in rename-map.json `entries[].legacy_name`
     - Replace with the `new_macro_owner_form` if the owning addon matches hal_boss, otherwise use `new_macro_extern_form`
     - For `publicVariable` string arguments, use the QGVAR/QEGVAR form
     - For `isNil "RydHQ_..."` guards, use QGVAR/QEGVAR string form
   - Replace any `call HAL_*` / `spawn HAL_*` bare global references with `call EFUNC(hal_hac,*)` / `spawn EFUNC(hal_hac,*)` (these will be extracted in Plan 03, but PREP'd before runtime)
   - Replace `call Boss` self-references (if any) with `call EFUNC(hal_boss,boss)`
   - Replace `call Desperado` with `call EFUNC(hal_hac,desperation)`
   - Register: add `PREP(boss);` to addons/hal_boss/XEH_PREP.hpp

2. **Front.sqf** (97 lines) -> `addons/core/functions/fnc_front.sqf`:
   - Add #include header, origin comment
   - Convert legacy variable names using rename-map.json (owner: core)
   - Register: add `PREP(front);` to addons/core/XEH_PREP.hpp

3. **Desperation.sqf** (48 lines) -> `addons/hal_hac/functions/fnc_desperation.sqf`:
   - Add #include header, origin comment
   - Convert legacy variable names using rename-map.json (owner: hal_hac)
   - Register: add `PREP(desperation);` to addons/hal_hac/XEH_PREP.hpp
   - Update the call site in addons/hal_hac/functions/fnc_statusQuo.sqf: replace `call Desperado` with `call FUNC(desperation)` (same addon)

4. **HQSitRep[B-H].sqf** (7 files, each ~688 lines) -> `addons/core/functions/fnc_HQSitRep[B-H].sqf`:
   - Each file: add #include header, origin comment
   - Convert legacy variable names using rename-map.json (owner: core)
   - Register all 7 in addons/core/XEH_PREP.hpp: `PREP(HQSitRepB);` through `PREP(HQSitRepH);`
   - NOTE: These are near-identical copies (dedup deferred to v1.1 per CONTEXT.md). Extract as-is.

5. Run `hemtt build` to verify. Expect possible warnings from un-updated call sites (HAL_* bare globals in the extracted files that reference Plan 03 scripts). These are acceptable at this stage — Plan 03 will resolve them.
  </action>
  <verify>
    <automated>cd C:/NAUKA/NR6-HAL && hemtt build 2>&1 | tail -10 && test -f addons/hal_boss/functions/fnc_boss.sqf && echo "boss exists" && grep -c "PREP(boss)" addons/hal_boss/XEH_PREP.hpp</automated>
  </verify>
  <done>Boss.sqf extracted as fnc_boss.sqf with all legacy variable names converted. Front.sqf, Desperation.sqf, and 7 HQSitRep variants extracted and PREP'd. All registered in XEH_PREP.hpp files. HEMTT build clean (or only HAL_* bare global warnings from not-yet-extracted Plan 03 files).</done>
</task>

<task type="auto">
  <name>Task 2: Extract HQ command scripts and update fnc_init.sqf dispatch</name>
  <files>
    addons/hal_hac/functions/fnc_flanking.sqf
    addons/hal_hac/functions/fnc_hqOrders.sqf
    addons/hal_hac/functions/fnc_hqOrdersDef.sqf
    addons/hal_hac/functions/fnc_hqOrdersEast.sqf
    addons/hal_hac/functions/fnc_hqReset.sqf
    addons/hal_hac/functions/fnc_lhq.sqf
    addons/hal_hac/functions/fnc_lPos.sqf
    addons/hal_hac/functions/fnc_reloc.sqf
    addons/hal_hac/functions/fnc_rev.sqf
    addons/hal_hac/functions/fnc_sfIdleOrd.sqf
    addons/hal_hac/functions/fnc_spotScan.sqf
    addons/hal_hac/XEH_PREP.hpp
    addons/core/functions/fnc_init.sqf
  </files>
  <action>
1. Extract 11 HQ-level command scripts from nr6_hal/HAL/ to addons/hal_hac/functions/:
   - Flanking.sqf -> fnc_flanking.sqf (183 lines)
   - HQOrders.sqf -> fnc_hqOrders.sqf (1187 lines)
   - HQOrdersDef.sqf -> fnc_hqOrdersDef.sqf (1038 lines)
   - HQOrdersEast.sqf -> fnc_hqOrdersEast.sqf (144 lines)
   - HQReset.sqf -> fnc_hqReset.sqf (535 lines)
   - LHQ.sqf -> fnc_lhq.sqf (88 lines, NOTE: call sites are in fnc_HQSitRep.sqf in core addon — use EFUNC(hal_hac,lhq))
   - LPos.sqf -> fnc_lPos.sqf (25 lines)
   - Reloc.sqf -> fnc_reloc.sqf (56 lines)
   - Rev.sqf -> fnc_rev.sqf (56 lines)
   - SFIdleOrd.sqf -> fnc_sfIdleOrd.sqf (66 lines)
   - SpotScan.sqf -> fnc_spotScan.sqf (168 lines)

2. For EACH file:
   - Add `#include "..\script_component.hpp"` header
   - Add origin comment: `// Originally from nr6_hal/HAL/{OriginalName}.sqf`
   - **Convert ALL legacy variable names** (RydHQ_*, RYD_*, RydxHQ_*, RydBB_*) to GVAR/EGVAR form using rename-map.json. The owner addon is hal_hac, so variables owned by hal_hac use GVAR(), others use EGVAR().
   - Replace any `call HAL_*` / `spawn HAL_*` internal references with FUNC() (same addon) or EFUNC() (cross-addon) macro calls. Map each HAL_* handle to its destination per the research inventory table.
   - Replace `call Boss` with `call EFUNC(hal_boss,boss)` if present
   - Replace `call Desperado` with `call FUNC(desperation)` if present (same addon)

3. Register all 11 in addons/hal_hac/XEH_PREP.hpp. Order does not matter for runtime-only cross-calls.

4. **Update fnc_init.sqf HQSitRep dynamic dispatch:**
   - Find the section that previously loaded HQSitRep functions from VarInit.sqf (the conditional `isNil "leaderHQ*"` blocks from VarInit.sqf Section C, lines 1130-1196)
   - This logic must be preserved in fnc_init.sqf. Add BEFORE the dispatch loop at line ~183:
     ```sqf
     A_HQSitRep = EFUNC(core,HQSitRep);
     if !(isNil "leaderHQB") then { B_HQSitRep = EFUNC(core,HQSitRepB); };
     if !(isNil "leaderHQC") then { C_HQSitRep = EFUNC(core,HQSitRepC); };
     // ... through H_HQSitRep
     ```
   - Update the `Boss` call site: replace `call Boss` (or `[args] call Boss`) with `[args] call EFUNC(hal_boss,boss)` at fnc_init.sqf:167
   - Update the `Front` call site: replace `call compile preprocessFile...Front.sqf` with `call EFUNC(core,front)` at fnc_init.sqf:138
   - Remove or comment out any remaining nr6_hal/ preprocessFile/execVM lines for Boss_fnc.sqf if still present (Phase 3 may have already extracted Boss_fnc.sqf content — verify by reading fnc_init.sqf:147)

5. **Update existing addons/ call sites** that reference these HAL_* handles as bare globals:
   - `grep -rn "HAL_HQOrders\b\|HAL_HQOrdersDef\b\|HAL_HQReset\b\|HAL_Flanking\b\|HAL_LHQ\b\|HAL_LPos\b\|HAL_Reloc\b\|HAL_Rev\b\|HAL_SFIdleOrd\b\|HAL_Spotscan\b" addons/`
   - Replace each hit with the appropriate EFUNC(hal_hac,...) macro
   - Known call sites from research: fnc_statusQuo*.sqf, fnc_HQSitRep.sqf, fnc_init.sqf

6. Run `hemtt build`. Verify zero errors.
  </action>
  <verify>
    <automated>cd C:/NAUKA/NR6-HAL && hemtt build 2>&1 | tail -10 && grep -c "PREP(hqOrders)" addons/hal_hac/XEH_PREP.hpp && grep -rn "call Boss\b" addons/ | grep -v compat | grep -v "EFUNC" | wc -l</automated>
  </verify>
  <done>11 HQ command scripts extracted as PREP'd functions in hal_hac. fnc_init.sqf dispatch updated: Boss -> EFUNC(hal_boss,boss), Front -> EFUNC(core,front), HQSitRep[A-H] variables populated with EFUNC references. All existing addons/ call sites for these HAL_* handles updated to EFUNC(). HEMTT build clean. Zero bare `call Boss` or `call HAL_HQOrders` references remain outside compat addon.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Boss.sqf and HQ command extraction</name>
  <action>Human verifies the AI commander core loop extraction is correct.</action>
  <what-built>Boss.sqf (2021-line main AI loop) extracted and all HQ command scripts migrated. This is the most behavior-sensitive extraction in the project — the AI commander's core decision loop and dispatch system are now CBA PREP'd functions.</what-built>
  <how-to-verify>
    1. Run `hemtt build` — should be zero errors, zero warnings (BBW1 accepted)
    2. Verify fnc_boss.sqf exists: `head -5 addons/hal_boss/functions/fnc_boss.sqf`
    3. Verify no bare legacy variable names in Boss: `grep -c "RydHQ_\|RydxHQ_\|RYD_" addons/hal_boss/functions/fnc_boss.sqf` — should be 0
    4. Verify no bare HAL_* call sites remain: `grep -rn "call HAL_\|spawn HAL_" addons/ | grep -v compat | grep -v "^Binary"` — should only show references to not-yet-extracted Plan 03 functions
    5. Verify HQSitRep dispatch in fnc_init.sqf: `grep "HQSitRep" addons/core/functions/fnc_init.sqf` — should show EFUNC references
    6. Spot-check one HQ command script: `head -5 addons/hal_hac/functions/fnc_hqOrders.sqf` — should have #include header
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Boss.sqf call dispatch | Boss.sqf calls HAL_* handles — all must resolve to PREP'd functions |
| HQSitRep dynamic dispatch | fnc_init.sqf resolves X_HQSitRep at runtime via getVariable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-03 | Tampering | Boss.sqf variable rename | mitigate | Grep for remaining RydHQ_/RYD_ in fnc_boss.sqf — must be zero |
| T-05-04 | Denial | HQSitRep dispatch | mitigate | Verify A_HQSitRep through H_HQSitRep variables are set before dispatch loop |
| T-05-05 | Denial | HAL_* undefined calls | mitigate | Grep for bare HAL_* in addons/ — only Plan 03 targets should remain |
</threat_model>

<verification>
- `hemtt build` exits 0
- `grep -rc "RydHQ_\|RydxHQ_\|RYD_\|RydBB_" addons/hal_boss/functions/fnc_boss.sqf` returns 0
- `grep -rn "call Boss\b" addons/ | grep -v compat | grep -v EFUNC` returns 0 lines
- `grep "PREP(boss)" addons/hal_boss/XEH_PREP.hpp` matches
- `grep "EFUNC(core,HQSitRepB)" addons/core/functions/fnc_init.sqf` matches
</verification>

<success_criteria>
Boss.sqf, Front.sqf, Desperation.sqf, 7 HQSitRep variants, and 11 HQ command scripts are all extracted, variable-renamed, and PREP'd in their respective addons. fnc_init.sqf dispatch is fully updated. Human has verified the extraction of the AI commander core loop.
</success_criteria>

<output>
After completion, create `.planning/phases/05-settings-localization-compat-cleanup/05-02-SUMMARY.md`
</output>
