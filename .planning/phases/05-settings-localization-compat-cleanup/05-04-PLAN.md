---
phase: 05-settings-localization-compat-cleanup
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - addons/hal_hac/functions/fnc_goAmmoSupp.sqf
  - addons/hal_hac/functions/fnc_goFuelSupp.sqf
  - addons/hal_hac/functions/fnc_goMedSupp.sqf
  - addons/hal_hac/functions/fnc_goRepSupp.sqf
  - addons/hal_hac/functions/fnc_suppAmmo.sqf
  - addons/hal_hac/functions/fnc_suppFuel.sqf
  - addons/hal_hac/functions/fnc_suppMed.sqf
  - addons/hal_hac/functions/fnc_suppRep.sqf
  - addons/hal_hac/XEH_PREP.hpp
  - addons/hal_tasking/functions/fnc_squadTasking.sqf
  - addons/hal_tasking/XEH_PREP.hpp
  - addons/core/functions/fnc_init.sqf
autonomous: true
requirements:
  - COMPAT-04
must_haves:
  truths:
    - "All 8 supply/support scripts are PREP'd in hal_hac"
    - "SquadTaskingNR6.sqf is PREP'd in hal_tasking"
    - "nr6_hal/ directory is completely empty of .sqf files (only Sound/ and config.cpp may remain temporarily)"
    - "Zero references to nr6_hal/ path remain in any addons/ code"
    - "The nr6_hal/ directory is deleted entirely"
    - "HEMTT build passes with zero errors"
  artifacts:
    - path: "addons/hal_hac/functions/fnc_suppAmmo.sqf"
      provides: "Ammo supply behavior (318 lines)"
      contains: "#include"
    - path: "addons/hal_tasking/functions/fnc_squadTasking.sqf"
      provides: "Squad task assignment engine (508 lines)"
      contains: "#include"
  key_links:
    - from: "addons/core/functions/fnc_init.sqf"
      to: "addons/hal_tasking/functions/fnc_squadTasking.sqf"
      via: "EFUNC(hal_tasking,squadTasking) replacing execVM SquadTaskingNR6.sqf"
      pattern: "EFUNC\\(hal_tasking,squadTasking\\)"
---

<objective>
Extract the final batch of nr6_hal/ scripts: 8 supply/support behavior files (GoAmmoSupp, GoFuelSupp, GoMedSupp, GoRepSupp, SuppAmmo, SuppFuel, SuppMed, SuppRep) and SquadTaskingNR6.sqf. Then delete the nr6_hal/ directory entirely, completing COMPAT-04.

Purpose: After this plan, nr6_hal/ no longer exists. All runtime code lives in addons/. This is the gate for the dead-var audit (Plan 05) and all subsequent plans.
Output: 9 new fnc_*.sqf files. nr6_hal/ directory deleted. Final build gate confirming zero nr6_hal/ references.
</objective>

<execution_context>
@C:\Users\jakub\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\jakub\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-settings-localization-compat-cleanup/05-CONTEXT.md
@.planning/phases/05-settings-localization-compat-cleanup/05-RESEARCH.md
@.planning/phases/05-settings-localization-compat-cleanup/05-03-SUMMARY.md
@addons/hal_hac/XEH_PREP.hpp
@addons/hal_tasking/XEH_PREP.hpp
@addons/core/functions/fnc_init.sqf
@nr6_hal/SquadTaskingNR6.sqf
@.planning/phases/04-variable-namespacing/rename-map.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract 8 supply scripts + SquadTaskingNR6.sqf</name>
  <files>
    addons/hal_hac/functions/fnc_goAmmoSupp.sqf
    addons/hal_hac/functions/fnc_goFuelSupp.sqf
    addons/hal_hac/functions/fnc_goMedSupp.sqf
    addons/hal_hac/functions/fnc_goRepSupp.sqf
    addons/hal_hac/functions/fnc_suppAmmo.sqf
    addons/hal_hac/functions/fnc_suppFuel.sqf
    addons/hal_hac/functions/fnc_suppMed.sqf
    addons/hal_hac/functions/fnc_suppRep.sqf
    addons/hal_hac/XEH_PREP.hpp
    addons/hal_tasking/functions/fnc_squadTasking.sqf
    addons/hal_tasking/XEH_PREP.hpp
    addons/core/functions/fnc_init.sqf
  </files>
  <action>
1. Extract 8 supply/support scripts from nr6_hal/HAL/ to addons/hal_hac/functions/:

| Source | Destination | Lines |
|--------|-------------|-------|
| GoAmmoSupp.sqf | fnc_goAmmoSupp.sqf | 604 |
| GoFuelSupp.sqf | fnc_goFuelSupp.sqf | 234 |
| GoMedSupp.sqf | fnc_goMedSupp.sqf | 223 |
| GoRepSupp.sqf | fnc_goRepSupp.sqf | 225 |
| SuppAmmo.sqf | fnc_suppAmmo.sqf | 318 |
| SuppFuel.sqf | fnc_suppFuel.sqf | 271 |
| SuppMed.sqf | fnc_suppMed.sqf | 299 |
| SuppRep.sqf | fnc_suppRep.sqf | 262 |

For EACH: #include header, origin comment, convert ALL legacy variable names using rename-map.json, replace HAL_*/RYD_* bare globals with FUNC/EFUNC macros. Register all 8 in hal_hac XEH_PREP.hpp.

2. Extract SquadTaskingNR6.sqf (508 lines) -> addons/hal_tasking/functions/fnc_squadTasking.sqf:
   - Add #include header, origin comment
   - Convert legacy variable names using rename-map.json (owner: hal_tasking)
   - Replace any HAL_*/RYD_* function calls with EFUNC macros
   - Register: add `PREP(squadTasking);` to addons/hal_tasking/XEH_PREP.hpp
   - Update fnc_init.sqf: replace `execVM (GVAR(path) + "SquadTaskingNR6.sqf")` at line ~205 with `[] spawn EFUNC(hal_tasking,squadTasking)` (it was originally execVM'd, so use spawn to preserve async behavior)

3. Update remaining call sites in addons/:
   - `grep -rn "HAL_GoAmmoSupp\|HAL_GoFuelSupp\|HAL_GoMedSupp\|HAL_GoRepSupp\|HAL_SuppAmmo\|HAL_SuppFuel\|HAL_SuppMed\|HAL_SuppRep" addons/`
   - Known: fnc_statusQuo.sqf (SuppAmmo/SuppFuel/SuppMed/SuppRep), fnc_action8ct-13ct (GoAmmoSupp/GoFuelSupp/GoMedSupp/GoRepSupp)
   - Replace with FUNC()/EFUNC() as appropriate

4. Delete the 3 already-migrated HAL/*.sqf files that have equivalents in addons/ (if not already deleted):
   - nr6_hal/HAL/EnemyScan.sqf (duplicate of core/fnc_enemyScan.sqf)
   - nr6_hal/HAL/HQSitRep.sqf (duplicate of core/fnc_HQSitRep.sqf)
   - nr6_hal/HAL/Personality.sqf (duplicate of core/fnc_personality.sqf)

5. Run `hemtt build` to verify zero errors.
  </action>
  <verify>
    <automated>cd C:/NAUKA/NR6-HAL && hemtt build 2>&1 | tail -10 && grep -rn "call HAL_\|spawn HAL_" addons/ | grep -v compat | grep -v "^Binary" | wc -l</automated>
  </verify>
  <done>All 8 supply scripts and SquadTaskingNR6.sqf extracted, variable-renamed, PREP'd. Zero remaining HAL_* bare globals in addons/. HEMTT build clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete nr6_hal/ directory and verify clean codebase</name>
  <files>
    nr6_hal/ (deleted)
  </files>
  <action>
1. Verify no remaining .sqf files in nr6_hal/ that haven't been extracted:
   ```bash
   find nr6_hal/ -name "*.sqf" -type f
   ```
   This should return zero files (all extracted in Plans 01-04).

2. Delete the entire nr6_hal/ directory:
   ```bash
   rm -rf nr6_hal/
   ```
   Note: Sound/ assets were copied to hal_data/Sound/ in Plan 01. config.cpp CfgRadio was extracted to hal_data/CfgRadio.hpp in Plan 01. Nothing unique remains.

3. Verify no references to nr6_hal/ path remain in addons/:
   ```bash
   grep -rn "nr6_hal\|NR6_HAL" addons/ | grep -v compat | grep -v "Originally from"
   ```
   Should return zero hits (excluding origin comments and compat addon references).

4. Check for any `GVAR(path)` references that previously pointed to nr6_hal/:
   ```bash
   grep -rn "GVAR(path)" addons/
   ```
   If fnc_init.sqf still sets `GVAR(path)` to the nr6_hal/ directory, remove or comment out that line. It's no longer needed.

5. Verify no bare `Boss` / `Desperado` / `A_HQSitRep`..`H_HQSitRep` globals remain as call targets (they should all be EFUNC'd now):
   ```bash
   grep -rn "call Boss\b\|call Desperado\b" addons/ | grep -v compat | grep -v EFUNC
   ```

6. Run final HEMTT build gate:
   ```bash
   hemtt build 2>&1
   ```
   Must be zero errors, zero warnings (BBW1 accepted). This build confirms the entire codebase compiles without nr6_hal/.

7. Count total PREP registrations across all addons to verify completeness:
   ```bash
   grep -c "PREP(" addons/*/XEH_PREP.hpp
   ```
   Document the count in the summary.
  </action>
  <verify>
    <automated>cd C:/NAUKA/NR6-HAL && test ! -d nr6_hal && echo "nr6_hal deleted" && hemtt build 2>&1 | tail -5 && grep -rn "nr6_hal" addons/ | grep -v compat | grep -v "Originally from" | grep -v "^Binary" | wc -l</automated>
  </verify>
  <done>nr6_hal/ directory deleted. Zero references to nr6_hal/ path in addons/ (except compat addon and origin comments). HEMTT build passes with zero errors. COMPAT-04 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nr6_hal/ removal | Deleting the directory must not leave orphaned references |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-05-08 | Denial | GVAR(path) pointing to deleted directory | mitigate | Remove GVAR(path) assignment from fnc_init.sqf |
| T-05-09 | Denial | Missed file in nr6_hal/ | mitigate | find nr6_hal/ -name "*.sqf" before deletion must return zero |
</threat_model>

<verification>
- `test ! -d nr6_hal` passes (directory deleted)
- `hemtt build` exits 0
- `grep -rn "nr6_hal" addons/ | grep -v compat | grep -v "Originally from" | grep -v "^Binary"` returns 0 lines
- `grep -rn "call HAL_\|spawn HAL_\|call Boss\b\|call Desperado\b" addons/ | grep -v compat | grep -v "^Binary"` returns 0 lines
</verification>

<success_criteria>
nr6_hal/ directory no longer exists. All code is in addons/. HEMTT build is clean. COMPAT-04 is fully satisfied. Zero orphaned references to the deleted directory.
</success_criteria>

<output>
After completion, create `.planning/phases/05-settings-localization-compat-cleanup/05-04-SUMMARY.md`
</output>
